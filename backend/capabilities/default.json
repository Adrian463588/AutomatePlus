{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Minimal local capability set for the AutomatePlus desktop window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
