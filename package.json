{
  "name": "automate-plus",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-First Multiplatform Test Automation & Code Generation Platform",
  "author": "AutomatePlus Core Team",
  "license": "MIT",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@automate-plus/desktop",
    "dev:desktop": "npm run dev --workspace=@automate-plus/desktop",
    "build:packages": "npm run build --workspace=@automate-plus/ir-schema && npm run build --workspace=@automate-plus/contracts && npm run build --workspace=@automate-plus/selector-engine && npm run build --workspace=@automate-plus/generators && npm run build --workspace=@automate-plus/persistence && npm run build --workspace=@automate-plus/recorder-web && npm run build --workspace=@automate-plus/recorder-android && npm run build --workspace=@automate-plus/runner-core && npm run build --workspace=@automate-plus/stress-engine",
    "build:desktop": "npm run build --workspace=@automate-plus/desktop",
    "build:sidecar": "npm run build --workspace=@automate-plus/sidecar",
    "verify:k6": "node scripts/verify-k6-online.mjs",
    "verify:k6:fixture": "npm run build:packages && node scripts/verify-k6-fixture.mjs",
    "verify:generators": "npm run build:packages && node scripts/verify-generators.mjs",
    "verify:ui-contract": "node scripts/verify-ui-contract.mjs",
    "e2e:online": "node scripts/verify-online-e2e.mjs",
    "e2e:android": "node scripts/verify-android-e2e.mjs",
    "verify:sidecar": "npm run build:packages && npm run build:sidecar && node scripts/verify-sidecar.mjs",
    "verify:docs": "node scripts/verify-markdown.mjs",
    "verify:authenticity": "node scripts/verify-authenticity.mjs",
    "build": "npm run build:packages && npm run build:sidecar && npm run build:desktop",
    "test": "vitest run",
    "typecheck": "npm run typecheck:packages && npm run typecheck:sidecar && npm run typecheck:desktop",
    "typecheck:packages": "tsc -p tsconfig.packages.json --noEmit",
    "typecheck:desktop": "tsc -p apps/desktop/tsconfig.json --noEmit",
    "typecheck:sidecar": "tsc -p apps/sidecar/tsconfig.json --noEmit",
    "lint": "node scripts/quality-check.mjs lint",
    "format:check": "node scripts/quality-check.mjs format:check",
    "format": "node scripts/quality-check.mjs format:check",
    "native:preflight": "node scripts/build-native-offline.mjs --preflight",
    "native:check": "node scripts/build-native-offline.mjs --check",
    "native:build": "node scripts/build-native-offline.mjs --build",
    "native:run": "scripts\\Run-AutomatePlus.bat"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
